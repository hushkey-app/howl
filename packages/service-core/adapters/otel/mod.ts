/**
 * Reference OpenTelemetry telemetry adapter. Storage-agnostic — the `db.system`
 * label and span-name prefix are configurable via its constructor.
 *
 * @module
 */
export { OpenTelemetryAdapter } from "./opentelemetry.adapter.ts";
