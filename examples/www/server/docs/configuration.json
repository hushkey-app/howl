{
  "slug": "configuration",
  "title": "Configuration",
  "description": "howl.config.ts, the Howl app, dev.ts and the HowlBuilder pipeline",
  "sections": [
    {
      "id": "overview",
      "heading": "Three files, three jobs",
      "blocks": [
        {
          "type": "p",
          "text": "A project is configured by three files, each with one job:"
        },
        {
          "type": "table",
          "headers": ["File", "Owns"],
          "rows": [
            ["howl.config.ts", "State interface, roles, defineConfig → { defineApi, config }"],
            [
              "server/main.ts",
              "new Howl(), engine registration, middleware, fsApiRoutes/fsClientRoutes"
            ],
            ["dev.ts", "HowlBuilder: esbuild, engine plugin, Tailwind, http-client generation"]
          ]
        }
      ]
    },
    {
      "id": "howl-config",
      "heading": "howl.config.ts",
      "blocks": [
        {
          "type": "p",
          "text": "Your State type and role union live here. defineConfig binds them once and returns a pre-typed defineApi (no per-file generics) plus a config for app.fsApiRoutes()."
        },
        {
          "type": "code",
          "lang": "ts",
          "filename": "howl.config.ts",
          "text": "import { defineConfig, memoryCache } from \"@hushkey/howl/api\";\n\n/** Per-request state shared across middleware and handlers. */\nexport interface State {\n  title: string;\n  userContext?: { id: string; roles: Role[] };\n}\n\nexport const roles = [\"USER\", \"ADMIN\"] as const;\nexport type Role = typeof roles[number];\n\nexport const { defineApi, config: apiConfig } = defineConfig<State, Role>({\n  roles,\n  cache: memoryCache({ maxSize: 500 }),\n  defaultRateLimit: { max: 100, windowMs: 60_000 },\n  getRateLimitIdentifier: (ctx) => ctx.state.userContext?.id,\n  checkPermissionStrategy: (ctx, allowedRoles) => {\n    const user = ctx.state.userContext;\n    if (!user) return ctx.json({ error: \"Unauthorized\" }, { status: 401 });\n    if (!allowedRoles.some((r) => user.roles.includes(r))) {\n      return ctx.json({ error: \"Forbidden\" }, { status: 403 });\n    }\n  },\n});"
        },
        {
          "type": "table",
          "headers": ["HowlApiConfig field", "Purpose"],
          "rows": [
            ["roles", "The valid role union for this app (required)."],
            [
              "checkPermissionStrategy",
              "Auth gate run before every role-protected endpoint. Return a Response to deny, return nothing to allow."
            ],
            [
              "defaultRateLimit",
              "Rate limit applied to every endpoint unless overridden per-route."
            ],
            ["cache", "Response cache adapter. Defaults to in-memory LRU."],
            [
              "rateLimitCache",
              "Adapter used only for rate-limit counters. Must be shared across instances in multi-instance deploys."
            ],
            [
              "getRateLimitIdentifier",
              "Resolves the per-user key for rate limits and per-user cache entries (e.g. ctx.state.userContext?.id)."
            ]
          ]
        },
        {
          "type": "warning",
          "text": "When getRateLimitIdentifier is unset or returns undefined, rate limiting falls back to the client IP and response caching is skipped on role-protected routes — without a per-user key a shared cache entry could leak one user's response to another."
        }
      ]
    },
    {
      "id": "the-app",
      "heading": "The Howl app",
      "blocks": [
        {
          "type": "p",
          "text": "Howl is one class; builder methods return this. Construct with HowlOptions, register an engine, add middleware and routes, export it."
        },
        {
          "type": "code",
          "lang": "ts",
          "filename": "server/main.ts",
          "text": "import { Howl, staticFiles } from \"@hushkey/howl\";\nimport { compression, coalesceRequests } from \"@hushkey/howl/middleware\";\nimport { vueEngine } from \"@hushkey/howl-vue\";\nimport { apiConfig, type State } from \"../howl.config.ts\";\n\nexport const app = new Howl<State>({\n  logger: true,            // timestamped, PID-tagged console output\n  engines: { vue: vueEngine() },\n});\n\napp.use(coalesceRequests());\napp.use(compression());\napp.use(staticFiles());\n\napp.use((ctx) => {\n  ctx.state.title = \"my-app\";\n  return ctx.next();\n});\n\napp.fsApiRoutes(apiConfig);   // crawl apis/**/*.api.ts\napp.fsClientRoutes();         // crawl pages/**\n\nexport default { app };"
        },
        {
          "type": "table",
          "headers": ["HowlOptions field", "Default", "Purpose"],
          "rows": [
            [
              "engines",
              "{}",
              "Map of registered render engines, e.g. { vue: vueEngine() } or { react: reactEngine() }."
            ],
            [
              "mode",
              "\"production\"",
              "Run mode. The builder sets this for you (development under dev, production under build)."
            ],
            ["basePath", "\"\"", "Path prefix every route is mounted under."],
            [
              "logger",
              "false",
              "Enable the built-in HowlLogger (timestamp + PID on console output)."
            ],
            ["debug", "false", "Enable debug logging (no-op when false)."]
          ]
        },
        {
          "type": "tip",
          "text": "app.configure(fn) lets you group setup into a callback and may be awaited if the callback is async — handy for opening a DB connection before registering middleware."
        }
      ]
    },
    {
      "id": "dev-ts",
      "heading": "dev.ts and the HowlBuilder",
      "blocks": [
        {
          "type": "p",
          "text": "HowlBuilder wraps esbuild with CSS Modules, automatic apis/ crawling, and per-engine page compilation. One file runs both dev (builder.listen) and build (builder.build) — branch on Deno.args."
        },
        {
          "type": "code",
          "lang": "ts",
          "filename": "dev.ts",
          "text": "import { HowlBuilder } from \"@hushkey/howl/dev\";\nimport { vuePlugin } from \"@hushkey/howl-vue/plugin\";\nimport { httpClientGenPlugin, tailwindPlugin } from \"@hushkey/howl/plugins\";\nimport { app } from \"./server/main.ts\";\nimport type { State } from \"./howl.config.ts\";\n\nconst builder = new HowlBuilder<State>(app, {\n  root: import.meta.dirname ?? \"\",\n  importApp: () => app,\n  outDir: \"dist\",\n  serverEntry: \"./server/main.ts\",   // apis/ resolved relative to this\n  clientEntry: \"./client/pages/_app.vue\",\n  plugins: [\n    vuePlugin(),                      // owns .vue files\n    httpClientGenPlugin({\n      apiDir: \"server/apis\",\n      outputFile: \"./generated/http-client.ts\",\n      aliases: { \"@server/\": \"server/\" },\n    }),\n  ],\n});\n\n// Tailwind attaches to a specific builder instance.\ntailwindPlugin(builder.getBuilder(\"default\")!);\n\nif (Deno.args.includes(\"build\")) {\n  await builder.build();\n} else {\n  await builder.listen({ port: 8000 });\n}"
        },
        {
          "type": "table",
          "headers": ["HowlDevOptions field", "Purpose"],
          "rows": [
            [
              "importApp",
              "Lazy loader for the app instance. Accepts the Howl instance or a { app } module shape."
            ],
            ["root", "Project root. Defaults to Deno.cwd()."],
            ["outDir", "Build output directory. Defaults to _howl; the scaffolder uses dist."],
            [
              "staticDir",
              "Source directory served by staticFiles(). Defaults to <root>/static; set it anywhere (relative to root, or absolute) to colocate assets, e.g. ./client/static."
            ],
            ["serverEntry", "Server entry path; apis/ is resolved relative to its directory."],
            [
              "clientEntry",
              "Client entry (e.g. ./client/pages/_app.tsx); pages crawl from the client root."
            ],
            [
              "plugins",
              "esbuild plugins. Always include the engine plugin (vuePlugin() / reactPlugin())."
            ],
            ["alias", "Import alias map passed straight to esbuild."]
          ]
        },
        {
          "type": "warning",
          "text": "The engine's esbuild plugin must be passed in plugins — it owns the .vue / .tsx page extensions and compiles them. The render engine (vueEngine/reactEngine) goes in the Howl constructor; the build plugin (vuePlugin/reactPlugin) goes in HowlBuilder. They are two halves of the same engine."
        },
        {
          "type": "warning",
          "text": "staticDir is the build half; app.use(staticFiles()) is the serve half. Setting (or defaulting) a populated staticDir without registering staticFiles() means the build still processes the assets but nothing serves them at runtime — they 404 silently. HowlBuilder.listen()/build() print a warning when a static dir has files but the middleware is missing, so the mismatch is loud instead of silent."
        },
        {
          "type": "tip",
          "text": "Client bundles tree-shake npm barrel imports automatically: importing a single named export from a package that declares \"sideEffects\": false (lucide-react/lucide-vue-next, date-fns, radix, …) pulls in only what you use, not the whole library. This works for both engines because it happens in the shared resolver, not the engine plugin. No per-icon deep imports needed."
        }
      ]
    },
    {
      "id": "hot-reload",
      "heading": "Hot reload in dev",
      "blocks": [
        {
          "type": "p",
          "text": "The dev server applies most edits inside the running process — no restart, no cold esbuild, no in-memory state lost. What it can reload it reloads in tens of milliseconds; what it cannot, it hands back to Deno's own watcher."
        },
        {
          "type": "table",
          "headers": ["You change", "What happens", "Restart?"],
          "rows": [
            [".vue page or component", "Incremental esbuild rebuild, browser reloads", "no"],
            ["CSS / static assets", "Transformed copies dropped, rebuilt on next request", "no"],
            ["A page added or deleted", "Routes re-crawled, router rebuilt in place", "no"],
            ["*.api.ts", "Definition re-imported, API routes rebound", "no"],
            [".ts / .tsx in the client tree", "Bundle rebuilt, then a restart is requested", "yes"],
            ["server/ modules, howl.config.ts", "Deno's watcher restarts the process", "yes"]
          ]
        },
        {
          "type": "p",
          "text": "One rule draws the line: Howl hot-reloads exactly what Deno's module cache does not own. A .vue file is compiled per request, so a new version costs nothing. A .ts module the runtime already imported is pinned in that cache — refreshing only the browser bundle would leave server-rendered markup a version behind the script that hydrates it, so the dev server asks for a restart rather than ship a mismatch."
        },
        {
          "type": "code",
          "lang": "json",
          "filename": "deno.json",
          "text": "{\n  \"tasks\": {\n    \"dev\": \"deno run -A --watch --watch-exclude=dist/,node_modules/,server/apis/ dev.ts\"\n  }\n}"
        },
        {
          "type": "p",
          "text": "Plain --watch (no \"=.\") watches only Deno's own module graph, which is precisely the set Howl cannot hot-reload — so the two watchers partition the project instead of racing over it. server/apis/ is excluded because Howl re-imports API definitions itself; letting the runtime restart on them would only be slower."
        },
        {
          "type": "tip",
          "text": "The terminal says which path ran: `_ hot reload client 52ms` means the process stayed up, `_ restarting — a module the runtime owns changed` means the edit needed a fresh one."
        },
        {
          "type": "warning",
          "text": "--watch-hmr is not recommended. Deno can hot-replace a leaf module, but Howl already reloads those files itself and rebinds the routes the runtime knows nothing about; anything with top-level side effects (server/main.ts) restarts either way."
        }
      ]
    },
    {
      "id": "tasks",
      "heading": "Project tasks",
      "blocks": [
        {
          "type": "p",
          "text": "Scaffolded deno.json wires four tasks:"
        },
        {
          "type": "table",
          "headers": ["Task", "Command", "What it does"],
          "rows": [
            ["dev", "deno run -A --watch ... dev.ts", "Serve with hot reload (see above)"],
            ["build", "deno run -A dev.ts build", "Production build into dist/"],
            ["start", "deno run -A dist/compiled-entry.js", "Run the built server"],
            [
              "compile",
              "deno compile -A ... dist/compiled-entry.js",
              "Produce a self-contained binary"
            ]
          ]
        }
      ]
    }
  ]
}
